/**
 * AttendanceTab — migrated from admin_1.html renderAttendanceTab().
 * Full implementation in next phase.
 */
export default function AttendanceTab({ tuitionId }) {
  return (
    <div className="text-center py-12 text-slate-400 text-sm">
      Attendance tab — coming next migration step.
      <div className="text-xs mt-2 text-slate-300">tuitionId: {tuitionId}</div>
    </div>
  )
}
